# Development Instructions

After implementing any code change, rebuild the app on the current machine.
When the rebuild succeeds, reinstall the newly built GitHub Desktop Custom app
in `/Applications`, relaunch it, and verify that the installed app matches the
new build and is running.
